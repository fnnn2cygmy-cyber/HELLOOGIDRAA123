-- ЗАГРУЗКА БИБЛИОТЕК
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Sense = loadstring(game:HttpGet('https://raw.githubusercontent.com/jensonhirst/Sirius/request/library/sense/source.lua'))()

-- ПЕРЕМЕННЫЕ
local player = game.Players.LocalPlayer
local camera = workspace.CurrentCamera
local mouse = player:GetMouse()
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

-- FOV КРУГ
local fovCircle = Drawing.new("Circle")
fovCircle.Thickness = 2
fovCircle.Radius = 100
fovCircle.Filled = false
fovCircle.Color = Color3.fromRGB(255, 0, 0)
fovCircle.Visible = false

-- ПЕРЕМЕННЫЕ AIMBOT
local aimbotEnabled = false
local aiming = false
local aimFov = 100
local aimParts = {"Head"}
local smoothing = 0.05
local wallCheck = true
local predictionStrength = 0.065
local currentTarget = nil
local currentTargetPart = nil

-- ПЕРЕМЕННЫЕ CHAMS
local chamsEnabled = false
local chamsConnection = nil
local chamsSettings = {
    EnemyColor = Color3.fromRGB(255, 0, 0),
    TeamColor = Color3.fromRGB(0, 0, 255),
    Transparency = 0.5
}

-- ПЕРЕМЕННЫЕ ANTI-AIM
local antiAim = false
local antiAimAmountX = 0
local antiAimAmountY = -100
local antiAimAmountZ = 0
local antiAimMethod = "Reset Velo"
local randomVeloRange = 100

-- ПЕРЕМЕННЫЕ SPIN-BOT
local spinBot = false
local spinBotSpeed = 20

-- ============================================
-- ФУНКЦИИ AIMBOT
-- ============================================
local function getClosestPart(character)
    local closestPart = nil
    local shortestDistance = aimFov
    
    for _, partName in ipairs(aimParts) do
        local part = character:FindFirstChild(partName)
        if part then
            local partPos, onScreen = camera:WorldToViewportPoint(part.Position)
            if onScreen then
                local distance = (Vector2.new(partPos.X, partPos.Y) - Vector2.new(mouse.X, mouse.Y)).Magnitude
                if distance < shortestDistance then
                    shortestDistance = distance
                    closestPart = part
                end
            end
        end
    end
    return closestPart
end

local function getTarget()
    local nearestPlayer = nil
    local nearestPart = nil
    local shortestDistance = aimFov
    
    for _, pl in pairs(game.Players:GetPlayers()) do
        if pl ~= player and pl.Character and pl.Character:FindFirstChild("Humanoid") and pl.Character.Humanoid.Health > 0 then
            local part = getClosestPart(pl.Character)
            if part then
                nearestPlayer = pl
                nearestPart = part
            end
        end
    end
    return nearestPlayer, nearestPart
end

local function aimAt(player, part)
    if player and part then
        local targetPos = part.Position
        local newCFrame = CFrame.new(camera.CFrame.Position, targetPos)
        camera.CFrame = camera.CFrame:Lerp(newCFrame, smoothing)
    end
end

-- ============================================
-- ФУНКЦИИ CHAMS
-- ============================================
local function applyChams(character)
    if not character or character == player.Character or not chamsEnabled then return end
    
    local pl = game.Players:GetPlayerFromCharacter(character)
    if not pl then return end
    
    local isEnemy = true
    if pl.Team == player.Team and pl.Team ~= nil then
        isEnemy = false
    end
    
    local color = isEnemy and chamsSettings.EnemyColor or chamsSettings.TeamColor
    
    for _, part in pairs(character:GetDescendants()) do
        if part:IsA("BasePart") and not part:FindFirstChild("Highlight") then
            local highlight = Instance.new("Highlight")
            highlight.Parent = part
            highlight.Adornee = part
            highlight.FillColor = color
            highlight.FillTransparency = chamsSettings.Transparency
            highlight.OutlineColor = color
            highlight.OutlineTransparency = 0.2
            part:SetAttribute("highlighted", true)
        end
    end
end

local function clearChams(character)
    if not character then return end
    for _, part in pairs(character:GetDescendants()) do
        if part:IsA("BasePart") and part:GetAttribute("highlighted") then
            local highlight = part:FindFirstChild("Highlight")
            if highlight then highlight:Destroy() end
            part:SetAttribute("highlighted", nil)
        end
    end
end

local function updateAllChams()
    if not chamsEnabled then
        for _, pl in pairs(game.Players:GetPlayers()) do
            if pl ~= player and pl.Character then
                clearChams(pl.Character)
            end
        end
        return
    end
    
    for _, pl in pairs(game.Players:GetPlayers()) do
        if pl ~= player and pl.Character then
            clearChams(pl.Character)
            applyChams(pl.Character)
        end
    end
end

-- ============================================
-- ОСНОВНЫЕ ЦИКЛЫ
-- ============================================
RunService.RenderStepped:Connect(function()
    if aimbotEnabled then
        fovCircle.Position = Vector2.new(mouse.X, mouse.Y + 50)
        
        if aiming then
            local target, targetPart = getTarget()
            if target and targetPart then
                currentTarget = target
                currentTargetPart = targetPart
                aimAt(target, targetPart)
            end
        end
    end
end)

RunService.Heartbeat:Connect(function()
    if antiAim then
        local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            if antiAimMethod == "Reset Velo" then
                local vel = hrp.Velocity
                hrp.Velocity = Vector3.new(antiAimAmountX, antiAimAmountY, antiAimAmountZ)
                task.wait()
                hrp.Velocity = vel
            elseif antiAimMethod == "Random Velo" then
                local vel = hrp.Velocity
                hrp.Velocity = Vector3.new(
                    math.random(-randomVeloRange, randomVeloRange),
                    math.random(-randomVeloRange, randomVeloRange),
                    math.random(-randomVeloRange, randomVeloRange)
                )
                task.wait()
                hrp.Velocity = vel
            end
        end
    end
end)

-- ============================================
-- УПРАВЛЕНИЕ AIMBOT (ПКМ)
-- ============================================
mouse.Button2Down:Connect(function()
    if aimbotEnabled then
        aiming = true
    end
end)

mouse.Button2Up:Connect(function()
    if aimbotEnabled then
        aiming = false
        currentTarget = nil
        currentTargetPart = nil
    end
end)

-- ============================================
-- СОЗДАНИЕ МЕНЮ
-- ============================================
local Window = Rayfield:CreateWindow({
    Name = "@HelloGidra | Hub",
    Icon = 128216856751714,
    LoadingTitle = "waiting the script loading",
    LoadingSubtitle = "by @HelloGidra",
    Theme = "Ocean",
    ConfigurationSaving = {
        Enabled = false
    },
    Discord = {
        Enabled = false,
        Invite = "noinvitelink"
    },
    KeySystem = false
})

-- ГЛАВНАЯ ВКЛАДКА
local HomeTab = Window:CreateTab("Home", nil)
HomeTab:CreateSection("About script")

HomeTab:CreateButton({
    Name = "Telegram channel",
    Callback = function()
        setclipboard("https://t.me/HelloGidra")
    end
})

-- ВКЛАДКА CHARACTER
local CharTab = Window:CreateTab("Character", "person-standing")
CharTab:CreateSection("Character")

CharTab:CreateSlider({
    Name = "Walk Speed",
    Range = {1, 350},
    Increment = 1,
    CurrentValue = 16,
    Callback = function(v)
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.WalkSpeed = v
        end
    end
})

CharTab:CreateSlider({
    Name = "Jump Power",
    Range = {1, 350},
    Increment = 1,
    CurrentValue = 50,
    Callback = function(v)
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.JumpPower = v
        end
    end
})

CharTab:CreateSlider({
    Name = "Gravity",
    Range = {1, 350},
    Increment = 1,
    CurrentValue = 196,
    Callback = function(v)
        workspace.Gravity = v
    end
})

CharTab:CreateButton({
    Name = "Sit",
    Callback = function()
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.Sit = true
        end
    end
})

-- ВКЛАДКА FUNCTIONS
local FuncTab = Window:CreateTab("Functions", "parentheses")

-- ESP
FuncTab:CreateSection("ESP")
FuncTab:CreateToggle({
    Name = "ESP",
    CurrentValue = false,
    Callback = function(v)
        if v then
            Sense.teamSettings.enemy.enabled = true
            Sense.teamSettings.enemy.box = true
            Sense.teamSettings.friendly.enabled = true
            Sense.teamSettings.friendly.box = true
            Sense.Load()
        else
            Sense.Unload()
        end
    end
})

FuncTab:CreateToggle({
    Name = "Box Fill",
    CurrentValue = false,
    Callback = function(v)
        Sense.teamSettings.enemy.boxFill = v
        Sense.teamSettings.friendly.boxFill = v
        if v then Sense.Load() end
    end
})

FuncTab:CreateToggle({
    Name = "Name",
    CurrentValue = false,
    Callback = function(v)
        Sense.teamSettings.enemy.name = v
        Sense.teamSettings.friendly.name = v
        if v then Sense.Load() end
    end
})

FuncTab:CreateToggle({
    Name = "Distance",
    CurrentValue = false,
    Callback = function(v)
        Sense.teamSettings.enemy.distance = v
        Sense.teamSettings.friendly.distance = v
        if v then Sense.Load() end
    end
})

-- FUNCTIONS
FuncTab:CreateSection("Functions")
FuncTab:CreateToggle({
    Name = "No Clip",
    CurrentValue = false,
    Callback = function(v)
        if v then
            noclip = RunService.Stepped:Connect(function()
                if player.Character then
                    for _, part in pairs(player.Character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        else
            if noclip then noclip:Disconnect() end
        end
    end
})

-- CHAMS
FuncTab:CreateSection("CHAMS")
FuncTab:CreateToggle({
    Name = "CHAMS (Командные цвета)",
    CurrentValue = false,
    Callback = function(v)
        chamsEnabled = v
        if v then
            chamsConnection = game.Players.PlayerAdded:Connect(function(pl)
                pl.CharacterAdded:Connect(function(char)
                    task.wait(0.5)
                    applyChams(char)
                end)
            end)
            for _, pl in pairs(game.Players:GetPlayers()) do
                if pl ~= player then
                    pl.CharacterAdded:Connect(function(char)
                        task.wait(0.5)
                        applyChams(char)
                    end)
                end
            end
        else
            if chamsConnection then
                chamsConnection:Disconnect()
                chamsConnection = nil
            end
        end
        updateAllChams()
    end
})

FuncTab:CreateColorPicker({
    Name = "Цвет врагов (CHAMS)",
    Color = chamsSettings.EnemyColor,
    Callback = function(c)
        chamsSettings.EnemyColor = c
        if chamsEnabled then updateAllChams() end
    end
})

FuncTab:CreateColorPicker({
    Name = "Цвет союзников (CHAMS)",
    Color = chamsSettings.TeamColor,
    Callback = function(c)
        chamsSettings.TeamColor = c
        if chamsEnabled then updateAllChams() end
    end
})

FuncTab:CreateSlider({
    Name = "CHAMS Transparency",
    Range = {0, 1},
    Increment = 0.05,
    CurrentValue = 0.5,
    Callback = function(v)
        chamsSettings.Transparency = v
        if chamsEnabled then updateAllChams() end
    end
})

-- AIMBOT
FuncTab:CreateSection("Aimbot")
FuncTab:CreateToggle({
    Name = "Aimbot",
    CurrentValue = false,
    Callback = function(v)
        aimbotEnabled = v
        fovCircle.Visible = v
    end
})

FuncTab:CreateDropdown({
    Name = "Aim Part",
    Options = {"Head", "HumanoidRootPart", "Left Arm", "Right Arm", "Torso", "Left Leg", "Right Leg"},
    CurrentOption = {"Head"},
    Callback = function(v)
        if type(v) == "table" then
            aimParts = v
        else
            aimParts = {v}
        end
    end
})

FuncTab:CreateSlider({
    Name = "Smoothing",
    Range = {0, 100},
    Increment = 1,
    CurrentValue = 5,
    Callback = function(v)
        smoothing = 1 - (v / 100)
    end
})

FuncTab:CreateSlider({
    Name = "Aimbot Fov",
    Range = {0, 1000},
    Increment = 1,
    CurrentValue = 100,
    Callback = function(v)
        aimFov = v
        fovCircle.Radius = v
    end
})

FuncTab:CreateColorPicker({
    Name = "Fov Color",
    Color = Color3.fromRGB(255, 0, 0),
    Callback = function(c)
        fovCircle.Color = c
    end
})

FuncTab:CreateToggle({
    Name = "Wall Check",
    CurrentValue = true,
    Callback = function(v)
        wallCheck = v
    end
})

-- ANTI-AIM
FuncTab:CreateSection("Anti-Aim")
FuncTab:CreateToggle({
    Name = "Anti-Aim",
    CurrentValue = false,
    Callback = function(v)
        antiAim = v
    end
})

FuncTab:CreateDropdown({
    Name = "Anti-Aim Method",
    Options = {"Reset Velo", "Random Velo"},
    CurrentOption = {"Reset Velo"},
    Callback = function(v)
        if type(v) == "table" then
            antiAimMethod = v[1]
        else
            antiAimMethod = v
        end
    end
})

FuncTab:CreateSlider({
    Name = "Anti-Aim Amount X",
    Range = {-1000, 1000},
    Increment = 10,
    CurrentValue = 0,
    Callback = function(v)
        antiAimAmountX = v
    end
})

FuncTab:CreateSlider({
    Name = "Anti-Aim Amount Y",
    Range = {-1000, 1000},
    Increment = 10,
    CurrentValue = -100,
    Callback = function(v)
        antiAimAmountY = v
    end
})

FuncTab:CreateSlider({
    Name = "Anti-Aim Amount Z",
    Range = {-1000, 1000},
    Increment = 10,
    CurrentValue = 0,
    Callback = function(v)
        antiAimAmountZ = v
    end
})

FuncTab:CreateSlider({
    Name = "Random Velo Range",
    Range = {0, 1000},
    Increment = 10,
    CurrentValue = 100,
    Callback = function(v)
        randomVeloRange = v
    end
})

-- SPIN-BOT
FuncTab:CreateSection("Spin bot")
FuncTab:CreateToggle({
    Name = "Spin-Bot",
    CurrentValue = false,
    Callback = function(v)
        spinBot = v
        local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            if v then
                player.Character.Humanoid.AutoRotate = false
                for _, obj in pairs(hrp:GetChildren()) do
                    if obj.Name == "Spinning" then obj:Destroy() end
                end
                local spin = Instance.new("BodyAngularVelocity")
                spin.Name = "Spinning"
                spin.Parent = hrp
                spin.MaxTorque = Vector3.new(0, math.huge, 0)
                spin.AngularVelocity = Vector3.new(0, spinBotSpeed, 0)
            else
                for _, obj in pairs(hrp:GetChildren()) do
                    if obj.Name == "Spinning" then obj:Destroy() end
                end
                player.Character.Humanoid.AutoRotate = true
            end
        end
    end
})

FuncTab:CreateSlider({
    Name = "Spin-Bot Speed",
    Range = {0, 1000},
    Increment = 1,
    CurrentValue = 20,
    Callback = function(v)
        spinBotSpeed = v
        local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if hrp and spinBot then
            for _, obj in pairs(hrp:GetChildren()) do
                if obj.Name == "Spinning" then
                    obj.AngularVelocity = Vector3.new(0, v, 0)
                end
            end
        end
    end
})

-- ВКЛАДКА SETTINGS
local SettingsTab = Window:CreateTab("Settings", "settings")
SettingsTab:CreateSection("Управление")
SettingsTab:CreateButton({
    Name = "❌ ОТКЛЮЧИТЬ ВСЁ",
    Callback = function()
        Sense.Unload()
        chamsEnabled = false
        if chamsConnection then
            chamsConnection:Disconnect()
            chamsConnection = nil
        end
        if noclip then noclip:Disconnect() end
        aimbotEnabled = false
        fovCircle.Visible = false
        antiAim = false
        spinBot = false
        local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            for _, obj in pairs(hrp:GetChildren()) do
                if obj.Name == "Spinning" then obj:Destroy() end
            end
            if player.Character then
                player.Character.Humanoid.AutoRotate = true
            end
        end
        workspace.Gravity = 196
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.WalkSpeed = 16
            player.Character.Humanoid.JumpPower = 50
        end
        Rayfield:Notify({
            Title = "❌ Всё отключено!",
            Content = "Все функции деактивированы",
            Duration = 3
        })
    end
})

SettingsTab:CreateButton({
    Name = "🔄 Перезагрузить скрипт",
    Callback = function()
        Rayfield:Notify({
            Title = "🔄 Перезагрузка...",
            Content = "Скрипт будет перезагружен",
            Duration = 2
        })
        task.wait(1)
        loadstring(game:HttpGet("https://raw.githubusercontent.com/fnnn2cygmy-cyber/HELLOOGIDRAA123/refs/heads/main/HELLOOGIDRAA123"))()
    end
})

-- УВЕДОМЛЕНИЕ
Rayfield:Notify({
    Title = "@HelloGidra Script Executed!",
    Content = "Добро пожаловать в хаб!",
    Duration = 5
})

print("[HelloGidra] Скрипт загружен и работает!")
